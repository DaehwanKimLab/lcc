// NADBiosynthesis_fromTryptophan
// input: tryptophan
// intermediate: quinolinate
// output: NAD
// DB: KEGG
// - M00912
pathway NADBiosynthesis_fromTryptophan
{
    reaction tryptophan2quinolinate(tryptophan --> quinolinate);
    reaction quinolinate2NAD(quinolinate --> NAD);
}
