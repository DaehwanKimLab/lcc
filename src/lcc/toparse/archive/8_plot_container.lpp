array S_list = [0.3, 1, 10];
for (int i = 0; i < len(S_list); i++)
{
    container tube
    {
        protein E(S --> P, k=1, krev=0.1);

        E[0]=1;
        S[0]=S_list;

        plot(S, P, type=Dynamics, unit=molecule);
        plot(S, P, type=PhasePlane);
        plot(S, P, type=Dynamics&PhasePlane); // allow multiple types?
    }
}
